import {
  comPrimary,
  playPrimary,
  playSecondary,
  playTertiary,
  playQuaternary,
  shopPrimary,
  tonePrimary,
} from './constants';

const logoMap = {
  com: { primary: comPrimary },
  play: {
    primary: playPrimary,
    secondary: playSecondary,
    tertiary: playTertiary,
    quaternary: playQuaternary,
  },
  shop: { primary: shopPrimary },
  start: { primary: comPrimary },
  tone: { primary: tonePrimary },
};

export default logoMap;
